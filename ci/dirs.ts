import * as path from 'path'

const dirs = {
    projectRoot: path.resolve(__dirname, '../..'),
    views: 'views',
    styles: 'styles',
    build: 'build/src',
    dist: 'dist'
}

export default dirs
